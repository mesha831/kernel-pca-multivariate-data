# kernel-pca-multivariate-data
The dataset used in thThe code is provided to demonstrate the Kernel PCA, ICA, and SVM. Users may substitute their own dataset with the same structure to reproduce the analysis. 
